package org.example.model;

import org.example.enums.Gender;

public class Principal extends Person{

    public Principal(String firstName,String lastName, int age, Gender gender) {
        super(firstName, lastName, age, gender);
    }
}
